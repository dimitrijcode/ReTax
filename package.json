{
  "name": "retax",
  "private": true,
  "packageManager": "pnpm@10.17.1",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm -r build",
    "test": "pnpm -r --if-present test",
    "typecheck": "pnpm -r --if-present typecheck",
    "fixtures:pdf": "node scripts/generate-laptop-invoice.mjs"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
